# 0.4.0持仓可卖数量简写 · 2026-08-31

- 持仓与可卖数值相等时显示单值，不相等时显示“持仓（可卖）”，保留千分位；表头和说明同步调整。仅改变共享证券表展示，数量计算、账本和列宽不变。
- 245项测试及生产构建通过；新增相等、十进制字符串等值、不相等、可卖为0、两者为0的展示测试。本次未重做浏览器或Mac原生UI验收。
- Mac app/DMG构建及签名、镜像完整性校验通过。当前DMG SHA-256：`b5b6dc8916684e2f3e003a541ff7da8b6af2a673fcf88f4ee318a00a38893b32`。以下为旧构建历史。

# 0.4.0默认列宽与悬浮轻提示 · 2026-08-31

- 只读查询Mac正式账户SQLite的workspace.views字段，取得用户配置：chart165、dailyPnl85、change90、fiveDay/volumeRatio/turnover/amplitude/floatCap/volume80、marketValue/cyclePnl/flow100。浏览器保存值不同，本次以Mac配置设定系统默认。没有直接修改正式数据库或查询交易流水。
- 布局v5默认今日盈亏后紧跟本轮盈亏，主力净流入最后；旧布局只迁移指定位置关系，保留其他自定义列宽、显隐、各视图独立数据排序及账本。走势图取消手动宽度，最小165px并填充剩余宽度；SVG固定行高同步拉伸。新布局再次加载不重复保存迁移。
- 待对账、工作台/成交/草稿/历史修改/备份恢复/在线搜索错误统一为可逐条关闭的body悬浮消息栈，层级高于对话框，不占页面高度。成功消息3秒退出；错误警告保留到关闭或来源状态消失。同一负资金提醒关闭后切页/行情刷新不重复；关闭不清空业务校验状态。
- 240项自动化测试、格式检查和生产构建通过。覆盖新默认值、v4→v5幂等迁移、其他自定义宽度保留、自动剩余宽度计算、无走势图调宽入口、跨组件提示堆叠/关闭/清理、提示CSS定位、负资金关闭不改流水、既有成交/历史校验和草稿保护。保留已有大chunk构建提示。
- 浏览器真实检查：1920px窗口表格容器1882px，走势图722px且表格恰好填满；1280×720无整屏横纵溢出。无效添加触发错误，只有一条悬浮消息、没有页内重复错误；关闭前后对话框均高536.4px。列设置“恢复默认”预览与Mac数值一致，走势显示“自动”；预览未保存，恢复原窗口尺寸，未提交有效自选或交易/资金数据。
- Mac app/DMG重新打包，签名和镜像完整性校验通过；本次未重新执行Mac原生UI交互验收，浏览器检查不替代原生验收。当前DMG SHA-256：`a2f445de317a898d5543239560f3a8f87969d788231be9cca7718d0edf6e34ef`。以下为旧构建历史。

# 0.4.0添加自选模糊搜索 · 2026-08-31

- 添加自选支持名称、全拼、拼音首字母、部分代码；保留原有手动填写字段。候选列表沿用36px控件和紧凑排版，内部限高滚动；鼠标选中或方向键/Enter只填入，确认添加才保存。
- 231项前端测试、11项Rust常规测试、格式检查及生产构建通过。覆盖本地名称/全拼/简称/代码、ETF、大小写/空格、非法输入、去重、防抖、中文组字、旧请求丢弃、卸载、断网、ETF未确认分类及已有证券保护。保留既有打包chunk体积警告。
- 独立执行Rust `live_security_search`联网测试通过：贵州茅、guizhoumaotai、gzmt、maotai、600519均返回贵州茅台；5103包含510300 ETF。固定腾讯域名、参数编码、8秒超时、不跟随重定向、64KiB响应上限；GBK/Unicode字符串解析，不执行接口文本，排除不支持市场和品种。
- 浏览器实测全拼/简称检索、回车填入贵州茅台、ETF候选点击填入及已有类别保留，未提交自选或账本记录。1280×720窗口下双候选列表弹窗高602.4px，文档尺寸仍1280×720；选中ETF后收起候选，弹窗高536.4px。已恢复原窗口尺寸与空白表单。
- 本次未重做Mac原生弹窗交互验收；浏览器验证与真实Rust搜索请求分别执行，不将其当作原生UI验收。Mac app/DMG已重新构建，包含此前弹窗自适应高度、文案删除及红涨绿跌调整；签名与DMG完整性校验通过。当前DMG SHA-256：`bee6f3a105dc5a929d16b8cc6682ff746b2a8f35f4cadde7803dea24cb50954e`。以下为旧构建历史。

# 0.4.0全局控件与表单一致性 · 2026-08-31

- 新增controls.css共享层，清理旧全局输入/按钮样式与局部字号覆盖。统一36px表单、32px工具栏/图标、28px快捷操作和16px复选框；数值字体与普通文字分离，补齐禁用、只读、焦点、悬停及减少动态效果状态。
- 修复分组六项操作错行、创建按钮顶对齐、复选框全宽、双行表头被flex影响、嵌入资金弹窗页脚泄漏及重复模态层级。列设置固定标题和保存区，仅字段列表滚动；交易操作区吸底。费率页面采用一致网格，窄窗口单列；备份页面增加有序的标题/操作布局。
- 202项测试、格式检查和生产构建通过。新增6项CSS级联回归与2项工作台语义/分组标签测试。CSS测试不替代真实布局；仍保留既有打包chunk体积警告。
- 浏览器逐页检查添加自选、列设置、分组、成交（估算及实际费用）、资金录入、历史修改、做T、费率及备份。表单输入/下拉实测36px/13px；工具栏32px；列宽输入与排序按钮32px；复选框16px；分组输入与创建按钮同为36px且top一致。做T内部输入/按钮34px+外框，总高36px。
- 1280×720交易弹窗买卖按钮36px、底部665px可见，实际费用三列等宽；列设置保存按钮固定可见，字段区域内部滚动。720×700工具栏换行、费率单列且document宽高不溢出。已恢复浏览器原尺寸；未提交任何成交、资金、费率、分组或列设置。
- 独立Mac副本“控件验收”启动成功，截图检查创业板ETF成交弹窗：输入/数量/日期/费用下拉与备注样式一致，买卖按钮对齐，手动成交保护及无底仓卖出禁用保留；原生QA只查看控件，不提交记录。
- Mac app签名和DMG完整性校验通过。当前DMG SHA-256：`b1dac5bea1017e9050624ced93eb6a31fcaa9681e32df4868f92e43d5b44851f`。共享规则与后续检查清单见UI_STYLE_GUIDE.md；以下为旧构建历史。

# 0.4.0证券标识与成交价跟随 · 2026-08-31

- 名称列120px、现价/成本100px，固定列偏移同步为120px；代码行显示SH/SZ及基/创/科，ETF不按名称误判为创业板股票。科创板仅展示层支持标识，证券录入/行情允许范围未扩大。
- 成交价默认每5秒跟随参考报价；手改、清空或行情按钮填价后锁定，可恢复跟随；草稿保存模式，旧非空草稿及显式载入旧方向草稿保护为手动。无效/错证券报价、隐藏窗口及保存期间不覆盖。延迟/缓存显式提示，历史成交仍需交割单核对。
- 194项测试及格式检查通过，生产构建通过（保留既有大包体积警告）。新增覆盖板块标识、120px宽度、5秒跟随、清空锁定、恢复跟随、旧/新草稿模式、股票/ETF精度及提交期间保护。
- 浏览器实测首两列120/100px、现价left=120px，SH/SZ/基代码行宽100px且不溢出。原成交草稿10元保留手动模式。1280×720时弹窗672px，买卖按钮底部660.5px，页面无整屏横纵滚动；已恢复窗口尺寸，未提交交易。
- 隔离Mac副本“行情验收”实测创业板ETF成交价自动填3.456，显示5秒跟随及延迟/缓存提示；手改3.123后保持锁定，恢复跟随后返回3.456。未提交成交/资金记录，随后关闭验收副本。测试时已收盘，动态报价变动及5秒时序由自动化测试覆盖；原生验证为真实参考报价填入与交互。
- Mac应用及DMG重新构建，签名与镜像完整性校验通过。当前DMG SHA-256：`90ec32b8394714d31da13b3ad7f2fbd79ab3acf3818186e43e80c145fca751c7`。以下为旧构建历史。

# 0.4.0先输入后选择买卖 · 2026-08-31

- 最终app签名与DMG校验通过，当前DMG SHA-256：`b8cd3051a2171ab3b7c9e9d28c8435f25c4516ab782a0d00db0e1d6574584bff`。独立Mac副本“交易验收”打开创业板ETF交易弹窗，实测焦点在成交价，共用股数/时间/费用输入，底部买入和卖出并排完整显示，无方向切换器；未填写或提交任何原生交易记录。
- 工具栏/右键/详情/历史页统一“交易”入口；弹窗先输入成交价、股数等，底部“买入”“卖出”决定最终方向；每边分别使用完整费用及账本校验预览现金收支。无底仓只禁用卖出；回车不选择默认方向，提交期间锁定双按钮及输入。
- 草稿改为每证券无方向entry键；旧方向草稿保留并显式载入，不自动选择或混用。提交成功清理本次使用草稿；清理失败保留已入账状态并禁止再次提交，重复成交确认限定买卖方向。实际费用及历史行情不变。
- 186项测试通过，覆盖共享输入双向提交、股票卖出税费、实际费用、无底仓/非法数量、无隐式回车提交、双击防重、跨方向重复检查、共享及旧草稿恢复、保存/清理失败、弹窗成功关闭与初始价格焦点。
- 浏览器实测外层只留交易按钮，弹窗焦点在成交价；ETF相同10元×100份输入显示买入支出1000.10、卖出收入999.90，双按钮可用，未提交记录；输入恢复为空。1280×720下弹窗高672，双按钮底部628px完整可见，document与viewport同宽高，无整屏滚动。浏览器已恢复原窗口尺寸。

# 0.4.0证券行右键菜单 · 2026-08-31

- 最终Mac app签名与DMG完整性校验通过，DMG SHA-256：`433e9d8792321e78a754ce39e6dc19848fd94043c7b54a9a275b948362cdd2db`。独立QA副本com.stev.tcalculator.menuqa实测右键万里石，显示对应菜单、全部操作及全选，末行下移禁用；Esc关闭及焦点恢复由浏览器验证。未执行记录/资金/移出操作，验收副本随后退出。
- 移除列表复选框/40px选择列及批量工具栏；名称固定列left=0、现价left=140，其他自定义列宽不变。右键菜单承接分组、移出、重排、全选、取消选择，并提供详情与买卖录入。Cmd/Ctrl点选、Shift按当前显示顺序连选，右键未选行切换为单只；多选时禁用单股详情、买卖及重排。
- 菜单自动避让边界，支持Shift+F10、上下/Home/End、Esc/Tab关闭与焦点恢复；外部点击、滚动、调整窗口、切换视图或筛选关闭。修复选中证券持久化后误关闭菜单的问题。移出保留确认，持仓页不允许手动重排。
- 176项自动化测试及格式检查通过。覆盖无复选框、列宽与偏移、Cmd/Shift选择、全选过滤、右键目标切换、批量分组、取消/确认移出、账本数据保留、分组隔离、重排、保存失败、键盘/菜单关闭。
- 浏览器实测：复选框数量0，名称无留白；右键创业板ETF目标正确，Shift连选/右键全选均显示3只；取消移出不改自选；Shift+F10与Esc后焦点回到股票名称，页面宽930且无整页横向溢出。未提交交易、资金或移出操作。

# 0.4.0用户指定涨跌配色 · 2026-08-31

- 最终app签名与DMG完整性校验通过。当前DMG SHA-256：`0b1083d1d797979b9b5291ddad07422626241edd5c58fc8317f8e38d4d0e7b49`；后续章节为旧构建历史。
- 涨#65019a、跌#48a579、平#8f9190，应用到表格、盈亏、做T结果、分时/五日分时、小图及日K/成交量；分时价格图例及面积填充与线色一致。按钮、均价/昨收辅助线和错误警示保留原色。
- 167项自动化测试、格式检查及生产构建通过。测试断言精确色值，浏览器读取已加载样式确认RGB为101/1/154、72/165/121、143/145/144。本次未复验原生图表，无账本数据改动。

# 0.4.0主副指标与百分比优化 · 2026-08-31

- 涨跌幅主、涨跌额副；今日/本轮盈亏金额主、收益率副。5日涨跌幅、换手率、振幅及盈亏率均随数字带%，盈亏正数加+；数量加千分位，主数值加重、副数值缩小，不改列宽或账本。
- 今日收益率采用盈亏÷〔日初市值＋max(买入总支出−卖出净收入,0)〕。日初基准缺失、盈亏未知或分母≤0显示—，详情可核对公式；未改费用及盈亏金额口径。
- 167项测试通过，覆盖百分比格式/主副顺序/零与负值、无持仓空值、今日收益率净买入/净卖出/当日建仓/清仓/零基数/缺失基准；格式检查与生产构建通过。
- 浏览器930px宽无整页横向溢出，所有自定义列宽保留。使用独立Mac副本com.stev.tcalculator.metricsqa与QA目录查看真实腾讯行情，目视确认各百分比后缀及双行排版；验收持仓今日盈亏-5.06、收益率-0.08%，点击明细后与6407.06买入支出基数相符。未修改成交资金记录，未重启正式窗口；验收副本已关闭。
- app签名及DMG完整性校验通过。当前DMG SHA-256：`7c8274157ede68c5619f2d1781accab800f8ef51352fc17917b45b317062a9bc`。以下为旧构建历史。

# 0.4.0去除重复数量列 · 2026-08-31

- 从默认列及可选列中移除独立持仓数量；布局v4迁移清理旧数量列、对应宽度与数量排序，其他字段顺序及自定义宽度保留，账本/证券/费率不变。
- 156项自动化测试通过，格式检查及生产构建通过；新增旧v3自选/持仓/分组迁移、幂等和数据引用不变检查。
- 浏览器实测自选、持仓均无独立数量列，市值下仍显示1300 / 1000，列设置中无数量选项。本次未复验原生交互，未重启正式窗口或修改交易记录。
- Mac app签名与DMG完整性校验通过。当前DMG SHA-256：`08edeac003ebfeeaa8095b78ca6ec0a1c8c04713b3e01bee3d840a6ccf2da393`。以下为旧构建历史。

# 0.4.0买卖入口、图表与跟随测算增量 · 2026-08-31

- 确认图表根因：抽屉外框438px，内部chart-wrap/画布只有110px；主坐标网格上32/下88px已超过画布高度。改为flex纵向填满图表外框，浏览器实测大窗口画布414px、1280×720时296px，三个周期一致。
- 买入/卖出按钮直接确定方向，表单隐藏切换器；独立方向草稿及同向旧草稿迁移。浏览器实测卖出入口标题/提交均为卖出，没有买卖切换器。
- 155项自动化测试、格式检查、生产构建通过。新增5秒跟随、单边锁定/清空、恢复跟随、ETF档位、数量/目标金额步进、隐藏与坏报价保护、历史成交不自动填价、跨方向草稿隔离及蓝涨青跌平灰检查。浏览器实测切换页签仍保留手动10.001及锁定提示。
- 使用本次构建的独立Mac副本（com.stev.tcalculator.chartqa，TCALCULATOR_QA_PROFILE=1）检查真实腾讯数据，未重启正式窗口或修改正式账户。逐个目视确认海格通信分时曲线、五日分时与日K/成交量/坐标轴/缩放区完整显示；日K蓝涨青跌。日K显示前复权缓存及获取时间，不隐瞒数据时效。
- Mac测算自动填入最新10.20；点击加一档变10.21并显示手动锁定，卖出价仍为10.20。仅计算预演，没有提交任何成交、资金或持仓操作。跨5秒价格变化由自动化控制报价测试覆盖，实机收盘后最新价不变。
- Mac app签名严格校验及DMG完整性校验通过。当前DMG SHA-256：`7d553b7c83e1c6388849d5c1bc771cbbb9e4215257afde9d197f50fa8cea2046`。以下摘要为旧构建历史。

# 0.4.0自选与持仓统一字段增量 · 2026-08-31

- 布局v3以旧“我的持仓”配置统一全部自选及分组的字段、顺序、列宽，保留各列表数据排序。后续列设置/拖动列宽同步，新分组继承；迁移不修改成交、资金与持仓内容。
- 150项自动化测试通过，生产构建通过。新增持仓基准迁移、联动设置、新分组继承、排序隔离、迁移幂等、未成交证券空值显示及已成交证券零收益保留测试。
- 浏览器实测切换全部自选、我的持仓、验收分组，表头文本、字段顺序与实际列宽逐项完全一致；刷新后仍保留。未交易证券的收益显示“—”，成本表头始终保留，避免按当前列表持仓情况变动字段。
- 未操作用户原生账户或重启原生窗口；本次交互验收为浏览器预览，不能替代原生复验。
- 格式检查通过，Mac app/DMG重新生成并通过签名及镜像完整性校验。当前DMG SHA-256：`67f99f6cee159cd08c5542236e083886dd0eb4f3a11896f3e2e464a39e8d27a5`，以下为旧构建历史。

# 0.4.0覆盖式详情抽屉增量 · 2026-08-31

- 详情由分栏改为固定右侧覆盖抽屉，默认640px宽，窄窗口最多窗口宽减32px；单击名称/行只选中，双击或名称Enter才打开，复选框双击不打开详情。保留行情、收益、做T与手工录入。
- 浏览器930×1276实测打开前后表格区域宽均894px，抽屉宽640px；1280×720下抽屉高720px，document尺寸与视口相同，做T区域与固定底部操作无横向裁切。
- 浏览器实测Shift+Tab从关闭按钮循环到底部录入买卖；从抽屉打开成交弹窗后Esc只关闭成交弹窗，焦点回到录入买卖；再次Esc收起抽屉。修复了inert解除之前恢复焦点导致焦点落到body的问题。
- 147项测试通过，新增单击/双击/Enter、复选框防误触、抽屉关闭、模态语义及嵌套Esc/焦点恢复。未录入或修改用户交易、持仓和资金，未重启用户原生窗口；本次原生交互未复验。
- 格式检查、生产构建通过；Mac app与DMG已重建并通过codesign深度严格校验及hdiutil校验。当前DMG SHA-256：`8efb7febb686b35582219077c64a8ae03816886f5e54b2bca9ad7fb21714bc89`，以下摘要为旧构建历史。

# 0.4.0紧凑指标增量 · 2026-08-31

- 只读查询桌面正式账户的workspace.views（不读取交易明细、不写数据库），确认用户在holdings保存了紧凑列宽与“今日收益”提前的排序。浏览器仍为上一版默认值，因此未把浏览器宽度误认为用户配置。
- 默认宽度据此设为走势188px、5日涨跌幅/主力净流入90px、持仓市值/本轮收益100px，其余80px；名称140px、现价/成本100px固定。布局v2迁移保留用户字段顺序与非默认自定义宽度，成本列合并入固定现价列，成本排序仍有效。
- 单位移入表头；流通市值统一亿元，成交量统一万手，涨跌率/收益率单元格不重复百分号。市值下只显示数量/估算可卖，股/份说明在表头提示。计算与费用口径未改。
- 144项TypeScript/Vue测试通过，格式及生产构建通过。新增单位换算展示、成本排序和合并、用户默认宽度、旧配置迁移/排序保留测试。
- 浏览器930×1276实测所有列宽准确，现价下显示原持仓成本9.7694、市值下显示1300 / 1000；刷新仍一致且无整页溢出。只验证独立浏览器预览，未修改原生持仓、资金、成交或手工配置，也未操作用户正在使用的原生窗口。
- 更新后的Mac app通过codesign深度严格校验，DMG通过hdiutil完整性校验。当前安装包SHA-256：`7c23242549a6d38c3bc49e9c78e96d93f449cc272bd204d101d1a6a6fbb78583`。以下摘要为旧构建历史；本次未复验原生交互。

# 0.4.0界面修正增量 · 2026-08-31

- 成交弹窗修复两处继承问题：主布局flex零基准导致表单高度塌缩；旧terminal的1040px最小宽度导致弹窗内容横向裁切。现明确弹窗高度与内层独立滚动，移除重复标题。
- 浏览器实测930×1276下弹窗高760px、内容宽774px，无横向溢出；1280×720下弹窗高672px，内层可见563px/内容645px，document尺寸仍等于视口，没有整页滚动。
- DOM实测名称140px、现价100px；涨跌幅/量比/换手率/振幅100px，5日涨跌幅/今日收益/成交量110px，主力净流入/流通市值/持仓市值/持仓成本单价/本轮持仓收益120px，走势图188px。
- 列设置真实指针拖拽：将走势移到5日涨跌幅后，保存并刷新仍保持；验收后通过上移按钮恢复原顺序。仅修改浏览器测试账本的布局配置，未录入或修改成交、资金与持仓，未操作原生正式账户。
- TypeScript/Vue 142项测试（13文件）通过，新增紧凑布局迁移幂等、列宽、双行展示、收益率零/负分母、指针拖拽/键盘移动/保存检查。格式检查与生产构建通过。
- Mac app和DMG已重新构建，codesign深度严格校验及hdiutil镜像校验通过；未退出或重启用户正在使用的原生窗口，本次弹窗/拖拽的原生交互仍未复验。
- 当前DMG SHA-256：`50ceb0365daafed2bf9acd0003f00b4872107411f96db5ebd3e1e9844749a098`。以下旧构建摘要仅作历史记录。

# 0.4.0重构验收 · 2026-08-31

- TypeScript/Vue共136项测试通过（12个文件），生产构建、格式检查通过。新增自选迁移/分组7项、日收益/持仓收益6项、批量行情调度4项、工作台集成4项；覆盖不等量买卖、手续费仅扣一次、过期报价、清仓后日收益、前复权与不复权隔离、分组重复/缺失引用、隐藏暂停、旧请求丢弃、退避、弹窗保存后列表同步、错误提示、草稿清理。
- Rust离线8项通过。另显式运行联网2项通过：6只沪深股票/不同类别ETF报价；4只证券各验证日K、不复权日K、分时、五日分时，共16组图表。报价文字按GBK解析，查询只携带公开证券代码。
- 浏览器真实交互：在独立预览账本创建“验收·做T关注”组，将沪深300ETF加入组，切换与刷新后保留；未删除证券或交易。该测试组保留于浏览器预览，原生正式账户不受影响。
- 浏览器不等量ETF测算：示例万1免5，10元买200份、10.1元卖100份，配对100份净收益9.80元、完整费用0.30元、净增仓100份、现金流−990.30元；保本10.002、开始盈利10.003、净赚10元目标价10.102。仅填写测算表单，不入账。
- 930×1276与1280×720下，document滚动尺寸均等于视口，表格与详情局部滚动。修复了条件行导致的高度错位、过宽筛选框、窄侧栏表单裁切、嵌入页面的错误提示隐藏、无尺寸的隐藏图表初始化。
- Mac应用通过独立 `TCALCULATOR_QA_PROFILE=1` 进程启动，原生WKWebView读取到v0.4工作台、空资金账本、真实股票/ETF报价、5日涨跌幅、量比、换手率、振幅、市值/成交量与分时小图。此进程使用qa-profile，不读写正式账户。检测到用户操作后停止自动点击，未在正式账户写入测试资金或成交。
- 实机验收仍有边界：原生录入→退出重开恢复、完整文件备份/恢复、断网/隐藏恢复全流程未完成；自动化与浏览器验证不能替代这些检查。当前用户操作中的原生窗口为隔离验收账本，正式使用应退出该进程后正常打开app。
- 最终`npm run package`成功：app为arm64，codesign深度严格校验通过；系统hdiutil生成只读DMG并通过校验。Tauri默认DMG外观脚本在临时挂载步骤后失败，已改为项目内可重复的系统镜像打包脚本，不依赖Finder自动化。临时挂载卷已卸载。
- 最终DMG SHA-256：`47b42cef8f643fdb6f19d7b4a94a45baa374b9d5460c94ebe43176fdb3c720d5`。本机ad-hoc签名，无付费签名或公证。
- 明确缺失：主力净流入显示“暂缺”及原因；不伪造资金流数据。ETF实际费率由用户确认，免5不等于免佣；分红送转需人工核对。

以下保留旧版验收历史，当前状态以上述v0.4记录和README为准。

# 0.3.1验收增量 · 2026-08-31

- 115项TypeScript/Vue测试通过：新增历史修订核心13项、工作台交互7项。覆盖历史费率不漂移、实际费用、FIFO重算、期初修改/删除、依赖持仓拦截、同时间稳定排序、非法记录、原数据不变、取消、失败不落内存、重复提交防护。
- Rust离线8项通过、2项联网测试跳过；TypeScript/Vite生产构建及格式检查通过。存储schema保持v3，兼容既有账户。
- 独立浏览器账本实测：卖出100份的价格从9.1改为9.2，现金从99,013.16变为99,023.16，FIFO已实现−90.09变为−80.09，同日配对收益9.82变为19.82。确认删除该卖出后，现金98,103.25，持仓1,200恢复为1,300，FIFO已实现和配对收益归零；刷新仍一致。
- 独立浏览器资金流水实测：修改逆回购利息3.52为5.52后删除，刷新后收入归零且记录消失，本金和股票持仓不受影响。仅删除了本工具先前创建的两条测试记录，没有修改用户原生账户；测试记录仍可按已知参数重新录入。
- 1280×720视口下页面滚动尺寸等于视口尺寸；删除确认截图已目视检查，目标摘要、前后金额、取消及确认按钮均清晰显示，长编辑弹窗局部滚动。
- 本次未在原生正式账户执行修改/删除，也未强制退出正在使用的窗口。新增功能原生操作→重启及文件备份恢复仍待实机验收，不能以浏览器测试替代。
- 最终v0.3.1的.app通过codesign深度严格校验、DMG通过hdiutil完整性校验。DMG SHA-256：5680d5807ac3c5faa80d2cd881052c4955feae40435bcb15aba6e68e336f0fbd。

以下为v0.3历史验收记录，功能限制以README当前版本为准。

# 0.3验收记录 · 2026-08-31

环境：Apple Silicon、macOS27.0、Xcode26.6、Rust1.87、Node24.18。

## 自动化

- TypeScript/Vue：95项通过。含既有费用/图表/持仓回归、新手工流水19项、工作台8项。
- 新增覆盖：空账本无需初始化、转入转出与收入分离、逆回购利息不污染FIFO、现金历史不全允许录入、回溯日期重算、历史持仓不足拦截、实际费用和6位均价/零股、费率修改不改变历史、v2余额无损迁移及幂等、非法金额/日期/重复ID、后补更早期初持仓。
- Rust：8项离线测试通过；2项联网测试本次默认跳过。v0.2已实测腾讯股票/多类ETF报价及12组日K/分时/五日接口，本次没有重新宣称全量联网回归。
- TypeScript检查、Vite生产构建、格式检查通过。ECharts超过500KB为体积提示，不是构建失败。

## 浏览器真实交互

使用与原生账户隔离的既有测试账本，旧余额迁移后保留原持仓与FIFO结果。通过界面分别补录：

- 2026-08-25 09:00转入1,000元。
- 同时间转出200元。
- 同时间国债逆回购利息3.52元。

账面现金从98,209.64变为99,013.16；净本金从110,000变为110,800；利息等收入与逆回购小计均为3.52；股票FIFO已实现仍为−90.09。刷新页面后资金流水、金额与持仓恢复一致。以上仅为独立测试数据，未写入用户原生账户。

1280×720视口下，document滚动尺寸与视口相同，无整页横向/纵向溢出；录入表单、自选与明细采用局部滚动。最终又移除了会覆盖表单的吸底按钮，并为紧凑图表分配独立的价格、成交量、时间轴和缩放区；最终版在930×1276视口再次检查，document尺寸与视口相同。

## Mac原生检查与交付

- 原生WKWebView读取到新版「证券账户账本」「手工同步 · 不连接券商」「录入历史成交」「资金流水」。
- 只读核对已存在的用户持仓与余额，未写入测试资金/成交，未删除原记录；已有余额保留为迁移来源。
- 真实腾讯分时曲线、均价、昨收、成交量和自选报价正常显示。截图：artifacts/mac-ledger-v3.jpeg（包含用户本机账面信息，请勿公开上传）。该截图记录最后一次紧凑布局微调之前的原生检查。
- .app为arm64，约8.2MB；DMG约3.7MB。本机ad-hoc签名，无公证。
- 最终.app通过codesign深度严格校验，DMG通过hdiutil完整性校验。DMG SHA-256：e83b649736768cd7aa1646be036834468775dc2ecc33e5fe116aa668d7d1226a。

## 实机验收边界

原生自动化点击曾间歇报“Sky Computer Use native pipe closed before response”，动作结果可能不确定。避免重复记账，本次原生检查只读，未在用户账户执行测试资金/成交。

尚未完成完整原生补录→退出→重开、文件选择器导出/恢复、拖动窗口缩放、断网/隐藏/恢复的全流程交互。浏览器交互与单元测试不能替代这些实机验收。最终布局微调已构建，但未强制退出用户正在使用的原生窗口来重拍。

## 明确口径与限制

新账户无初始化资金。历史现金不足是待核对提示，不阻止录入；历史总持仓不足阻止卖出。历史成交不做T+1执行审批，可卖数量仍只是上海日期隔日解锁估算，无交易日历。

国债逆回购仅记录到账净利息，不记录本金冻结/解冻；转账不计利润，利息不改变证券成本。成交无法直接编辑删除，JSON恢复为整账户替换。实际ETF佣金率仍须用户填写确认，最低佣金0。
# 资金流与成交BS点 · 2026-08-31

- 后续T标记调整：日K同日买卖合并为单个T（含不等量、先卖后买、多笔），仅单边交易保留B/S，跨日期不合并；悬停保留各笔方向，删除一边恢复B/S，分时和五日分时不合并。294项前端测试通过。本次为标记逻辑与组件回归检查，未新增浏览器或Mac原生视觉验收。
- 前端290项测试、Rust13项常规测试及生产构建通过，Mac app/DMG重新生成，签名与DMG完整性校验通过；保留既有大chunk构建提示。本次未重新执行Mac原生UI联调，浏览器视觉验证不替代该项。
- 新增东方财富固定域名资金流请求、50只批次上限、8秒超时、禁止重定向、响应体限制、证券身份/字段/金额口径/时间校验；金额字符串跨层，缺失不当零。前端独立30秒轮询、隐藏暂停、失败退避及缓存日期隔离，不影响腾讯价格通道。
- Rust常规测试13项通过（4项联网测试默认忽略）；本次单独执行 `fund_flows::tests::live_fund_flows -- --ignored --nocapture` 失败：连接失败或超时。东方财富官方页面脚本确认字段与正式域名，但不将接入代码、单元测试或此前成功响应当成本次联网成功。实时可用性待网络恢复后复核。
- BS标记覆盖股票/ETF、分时/五日/日K、上海时间、跨日空格索引、同点多笔、相反方向、前复权锚点、窗口外和建仓快照排除、修改删除及切换证券。未改任何账户数据或盈亏计算。
- 独立浏览器验收页复用真实MarketChart组件与正式样式，显式合成行情隔离账户。三种模式均截图检查到B/S标记；画布638×314，视口930px与文档宽一致，无横向溢出。此项不替代Mac原生图表与实时数据联调。
# 固定证券表列宽 · 2026-08-31

- 295项前端测试、格式检查和生产构建通过；Mac app及DMG重新打包，签名及镜像完整性检查通过。
- 只读查询正式Mac账户修订938的 `workspace.views`，未读取/输出成交和资金明细，未写入数据库。all、holdings及分组宽度一致。将主力净流入95、持仓市值96、本轮盈亏93与其他已有紧凑宽度固化。
- 表格宽度解析统一忽略旧自定义宽度，避免旧账户/恢复备份覆盖固定值；旧配置仍兼容，不触发仅为列宽的账本迁移。走势仍自动填充剩余宽度、最小165px。
- 删除表头指针调宽逻辑、调宽手柄及样式，列设置数值输入改为只读宽度说明；保留字段显隐、拖拽/键盘顺序与数据排序。
- 新增固定默认值、旧宽度不能覆盖、输入配置不被修改、自动剩余空间计算、无调宽入口及只读设置展示测试。本次未新增浏览器或Mac原生UI验收。
# 顶部收益同行展示 · 2026-08-31

- 今日收益及当前持仓轮次收益采用金额＋收益率同行、基线对齐，收益率保留12px/16px字号行高。窄窗口允许整项汇总换行，不拆开金额和收益率，不截断金额；盈亏口径、颜色和数据不变。
- 296项前端测试通过，新增同行容器、nowrap、基线对齐、字号和间距回归；本次未新增浏览器或Mac原生UI验收。
# 交易弹窗费用口径切换 · 2026-08-31

- 297项前端测试、格式检查及生产构建通过；Mac app/DMG已重新打包，签名和镜像完整性检查通过。
- 移除录入弹窗固定760px上限，改按内容撑高、最大为窗口可用高度。成交价/数量/时间并排，实际费用与口径选择同排，控件尺寸不变；窄窗口折行，极小窗口内容超出屏幕时仍允许必要的内部滚动，避免截断交易按钮。
- 浏览器1280×720实测：修改前实际费用模式弹窗672px，内部clientHeight561、scrollHeight690；修改后两种费用模式弹窗660.30px，内部clientHeight与scrollHeight均549，无内部滚动条。买入/卖出按钮底部655.15px，整页高度与滚动高度均720px。已截图检查实际费用输入与按钮完整可见。
- 测试切换actual→estimated→actual保留实际费用、成交价、时间，只有一个时间输入且不提交账户记录；浏览器验收恢复原actual选项并关闭弹窗，没有提交成交或资金流水。本次未执行Mac原生UI联调。
# 不占宽度的纵向悬浮滚动条 · 2026-08-31

- 304项前端测试、格式检查及生产构建通过；Mac app/DMG已打包，签名与镜像完整性校验通过。
- 根应用挂载统一悬浮滚动条组件：原生纵向宽度恒为0，横向高度不覆盖；仅滚动时显示固定定位的细条，900ms空闲后160ms淡出，减少动画偏好下不渐变。原生触控板/滚轮行为不拦截，补充拖动与键盘滚动；不包裹或重排业务DOM。
- 滚动区域身份、上下限、尺寸和祖先裁剪随实际容器计算；弹窗外点击、隐藏、窗口变化时隐藏，组件卸载移除监听器和临时ID。有限RAF测量，仅观察当前活动容器，不轮询全部页面元素。
- 正式预览列设置面板：改前内容宽659px/面板674px，修改后clientWidth674px与面板宽一致。默认没有悬浮条；关闭设置未保存字段或账户变更。
- 隔离验收页复用真实组件，截图确认滚动中细条可见，position=fixed、opacity=1；停止后opacity=0，可用宽均318px。End键到scrollTop877（等于max），横向scrollLeft80可用且不显示纵条；原生横向仍保留15px高度。拖动映射、计时、回到顶部、已有ID、不支持引擎回退及清理由单元测试覆盖。本次未执行Mac原生UI联调。
# 历史记录编辑弹窗 · 2026-08-31

- 历史修改表单与全账本重算预览改为左右两栏；时间与成交方向并排，保留36px输入控件、历史费率说明、实际费用提示和确认操作。移除重复小标题，收紧纵向留白。仅调整展示，不修改费用或账本重算逻辑。
- 浏览器1280×720实测：原弹窗clientHeight672、scrollHeight1000；修改后估算模式高度537.58px（clientHeight/scrollHeight均538），实际费用模式607.58px（均608），无横向或纵向溢出，保存按钮完整可见。建仓编辑509.98px、资金编辑482.39px、删除确认532.69px，均无纵向溢出。所有浏览器操作均取消，未保存账户更改。
- 宽度小于等于760px时改为单栏；弹窗按内容增高，最大不超过窗口可用高度。极小窗口或异常长内容仍保留必要的安全滚动，避免截断字段和操作；此次未实测窄窗口与Mac原生UI。
- 306项前端测试、格式检查及生产构建通过。新增双栏样式回归、历史费用口径来回切换保留价格/费用/时间、取消不保存的测试；既有修改删除与依赖校验测试保持通过。保留已有大chunk构建提示。
- Mac app/DMG已重新生成，本地签名及镜像完整性校验通过；未进行付费签名或公证，未重启用户正在使用的桌面应用。
# 右键加入多个分组 · 2026-08-31

- 将目标分组下拉框改为复选框列表，支持整行点击、键盘焦点、已选计数、多证券加入多分组。仅新增关联，已有分组保持不变，成员自动去重，选中分组一次保存；空选择不提交，取消不保存，失败保留选择。
- 308项前端测试及格式检查通过；覆盖多分组多证券、去重、未选分组保留、取消后重开重置、全部取消不提交、保存失败不改原数据。
- 生产构建及Mac app/DMG打包通过，本地签名与镜像完整性校验通过；保留既有大chunk提示，未进行公证。
- 浏览器1280×720检查右键菜单的键盘等效入口Shift+F10与真实分组选择弹窗：现有1个分组下高度258.90px，无内部溢出，复选框16px，选中整行高亮；截图核查后取消，未保存任何分组或账本改动。多个分组的提交逻辑由集成测试覆盖，未新增实际账户分组做浏览器测试。本次未进行Mac原生交互验收。
# 分组默认勾选 · 2026-08-31

- 单只证券打开右键加入分组时，按当前分组成员回填勾选；多只证券无论共同或部分归属，一律不预选。每次打开重新计算，不沿用取消前草稿；仍是只新增关联，不改变移出分组的独立操作。
- 309项前端测试及格式检查通过；覆盖单只多归属、空归属、多只共同归属不预选、取消后重开恢复与切换证券不串选。切换当前证券会保存界面状态，断言分组成员保持不变。
- 浏览器实测沪深300ETF右键弹窗默认勾选现有3个分组并显示“已选3个”，取消关闭，未保存分组更改。多证券情形由集成测试覆盖。本次未新增Mac原生交互验收。
- 生产构建、Mac app/DMG打包及镜像完整性检查通过，保持本地自用签名，未公证；已有大chunk提示未改变。
# 做T快捷入口与单只分组同步 · 2026-08-31

- 右键增加“做T测算”，单只证券直接进入对应详情抽屉的planner页，多选时禁用；复用已有测算，不创建成交记录。
- 单只分组由只新增改为勾选结果同步：勾选加入，取消勾选移出，点击“保存分组”后统一生效，允许全部取消；未改变时禁用保存。只移除该证券与分组的关联，保留分组本身、其他证券顺序、全部自选及交易。多只仍默认不选、只添加，不批量移除。
- 311项前端测试及格式检查通过，覆盖入口证券/页签准确性、多选禁用、单只增减与清空分组、不改变其他证券及历史数据，既有取消和失败保留测试通过。
- 浏览器已确认沪深300ETF右键操作进入做T测算页，未输入或提交交易；浏览器工具在页面切换后曾返回旧选择器超时，以后续可见DOM中的正确证券与测算表单确认结果。本次未进行Mac原生交互验收。
- 浏览器分组弹窗实测默认3项已选，取消全部后checked=0且保存按钮可用，最后取消关闭，未修改实际分组关联。生产构建、Mac app/DMG打包及镜像完整性校验通过，未公证。
# 证券板块与T+0标签 · 2026-09-01

- ETF以红底白字“基”替代SH/SZ，仅settlement明确为T+0时在代码后显示浅蓝色“T+0”；T+1与未确认均不额外标注。创业板300/301、科创板688股票分别以“创”“科”替代SZ/SH；主板股票继续显示SH/SZ。只按证券结构化字段和代码规则判断，不按名称猜测回转交易。
- 身份标签统一#C6414B红底白字；T+0使用#EAF1FF浅蓝底、#3268CF蓝字及细边框，保持第一列既有高度和110px宽度。
- 浏览器1280×720实测当前3行展示为“基510300”“基159915”“SH600519”，无横向挤压；两只ETF当前数据均为T+1，因此不显示T+0。T+0、创业板和科创板组合及标签title由组件测试覆盖，颜色由CSS级联测试覆盖。本次未新增Mac原生交互验收。
- 312项前端测试及格式检查通过；生产构建、Mac app/DMG打包及镜像完整性校验通过，保持本地自用签名，未公证。既有大chunk提示未改变。
# 行情3秒刷新 · 2026-09-01

- 列表批量行情、单证券报价、交易弹窗成交价及做T测算未锁定的买卖价由5秒改为3秒；所有界面文案同步。手动修改或清空后继续锁定，恢复跟随后再按3秒更新。
- 隐藏窗口暂停、恢复立即刷新、请求不重叠和代码切换丢弃旧响应保持不变。失败重试以3秒为基数指数退避并保留60秒上限；批量首轮失败由10秒改为6秒重试。K线/分时完整数据仍按30秒刷新，列表最新价可实时补当日日K，资金流仍按30秒，避免将慢接口提升到3秒。
- 312项前端测试及格式检查通过；轮询周期、退避、隐藏恢复、手工锁定与恢复跟随测试均已更新。
- 生产构建、Mac app/DMG打包及镜像完整性校验通过，保持本地自用签名，未公证；已有大chunk提示未改变。
# 当前持仓轮次交易日 · 2026-09-01

- 股票/ETF名称后增加“n交易日”，从当前持仓轮次建仓日开始，按腾讯不复权日K的实际交易日期计数并包含建仓当日；使用结构化成交日期，不按自然日、周末或节假日猜测。
- 轮次边界与盈亏口径一致：同一交易日内清仓再买入不重开；日终为0后隔日重新买入从1交易日开始。清仓当日保留行时仍显示刚结束轮次天数。仅为当前持仓/清仓当日证券计算，在全部自选和分组中同一证券也显示相同天数。
- 日K默认覆盖180个交易日；若建仓日早于当前缓存首日、行情离线或不复权数据缺失，则不显示天数，避免低估。当天有成交但日K尚未生成完整当日柱时，以成交记录确认当天是交易日。
- 314项前端测试、格式检查及生产构建通过；覆盖周末间隔、同日清仓再买、跨日新轮次、行情覆盖不足、空行情和名称后标签说明。
- Mac app/DMG重新打包并通过镜像完整性校验，保持本地自用签名，未公证；已有大chunk提示未改变。本次未新增Mac原生UI验收。
# 做T测算正算与反推 · 2026-09-01

- 将做T测算拆成两个独立卡片。正算独立输入买入价/数量、卖出价/数量，直接计算配对净收益、净增减仓、现金流、完整费用及测算后持仓；反推独立输入方向、固定价、买卖数量和目标净利润，输出合法档位上的目标价、保本价、开始盈利价和五档利润。两区价格、数量和手工锁定状态互不影响。
- 正算和反推均以当前轮次持仓数量、当前轮次净投入为基数，将完整买入支出和卖出净收入计入后，输出测算后持仓数量、成本金额、摊薄成本单价、按当前参考现价计算的持仓市值、持仓收益及收益率。未配对数量作为真实增减仓计入，不把全部现金流冒充配对利润。
- 测算后清仓时本轮持仓收益/成本单价显示为空；卖出导致负持仓时明确不可计算；无参考现价时仍计算数量、成本金额和摊薄成本，但不伪造收益。所有测算只读，不写入成交、持仓或资金流水。
- 浏览器1280×720确认抽屉内正算/反推为两个有边界的独立卡片，各自说明和输入清晰；当前浏览器离线无有效价格时两个卡片分别显示价格校验提示，未写入账户。抽屉使用既有内部滚动，底部交易操作保持固定可见。
- 318项前端测试、格式检查及生产构建通过；新增等量做T、不等量增仓、清仓、负持仓、缺少参考价、独立输入/结果与反推方向测试。保留既有大chunk提示。
- Mac app/DMG重新打包并通过镜像完整性校验，保持本地自用签名，未公证。本次未新增Mac原生UI验收。
# 顶部指标构成卡片 · 2026-09-01

- 依照用户提供的券商界面参考，将六项“？”从小型文字浮层改为半透明全屏遮罩上的居中构成卡片；卡片使用当前桌面版灰蓝视觉，支持关闭按钮、点击遮罩、Escape关闭及焦点返回，不占用顶部摘要高度。
- 每项改为真实金额明细：总资产列持仓市值与账面资金；账面资金列转入/迁移余额、卖出净收入、利息收入、转出和买入支出；今日收益列日初/日末市值、当日买卖净额与已含手续费；本轮收益列市值与净投入；利息按逆回购、普通利息、其他收入拆分。
- 浏览器现有账本实测“账面资金构成”为+101,000.00转入、−200.00转出、−2,700.27买入支出，合计98,099.73，与顶部一致；面板470×418px、六行明细、未增加整页滚动。今日收益构成卡片392px高且无内部滚动。验收未写入账户。
- 319项前端测试、格式检查及生产构建通过；覆盖六个入口、遮罩对话框语义、资金明细、关闭与账户不写入，保留既有大chunk提示。
- Mac app/DMG重新打包并通过镜像完整性校验，保持本地自用签名，未公证。本次未新增Mac原生UI验收。
# 做T测算切换布局 · 2026-09-01

- 正算与反推改为同一内容区的双页签切换，默认显示正算，同一时间只渲染一套输入和结果；两边输入、数量及价格锁定状态独立保留，切换不重置，也不改变既有计算口径。
- 桌面宽度下，当前页签内部采用左侧输入、右侧结果的紧凑布局；窄窗口自动回退单列。反推五档价格与持仓指标保留完整，没有通过隐藏结果换取高度。
- 浏览器1280×720实测：完整正算结果卡片高421.5px，完整反推结果卡片高409.5px；两种模式的测算容器clientHeight与scrollHeight均为506px，不再产生纵向滚动。测试输入仅用于预览，未写入账户，验收后已关闭抽屉并恢复窗口尺寸。
- 319项前端测试、格式检查及生产构建通过；新增页签可访问状态、单页渲染、切换后数据保持验证，保留既有大chunk提示。
- Mac app/DMG重新打包并通过镜像完整性校验，保持本地自用签名，未公证。本次未新增Mac原生UI验收。
# 顶部账户指标说明 · 2026-09-01

- 账面总资产、账面资金、持仓市值、今日收益、当前持仓轮次收益、利息等收入六项均增加“？”说明入口；说明以浮层呈现，不占用摘要区高度，支持点击、键盘焦点、Escape和点击外部关闭。
- 文案按实际账本口径说明资金组成、成交净收支、行情市值、收益及收益率分母；明确账面资金不等于券商可用/可转资金，利息等收入已计入账面资金和总资产，避免重复理解。
- 319项前端测试、格式检查及生产构建通过；浏览器预览实测六个入口完整，浮层宽310px、未超出视口，摘要区高度保持66.5px，未新增整页滚动。
- Mac app/DMG重新打包并通过镜像完整性校验，保持本地自用签名，未公证。本次未新增Mac原生UI验收。
