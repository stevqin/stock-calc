# T刻控件规范与复验清单

当前入口为 `src/main.ts → TradingDesk.vue`，共享工作台为 `LedgerWorkbench.vue`。`App.vue`、`Workspace.vue`、`style.css` 是旧版实现，不是当前桌面入口；本次不删除旧源码，也不迁移账户数据。

## 统一规则

控件外观由 `src/controls.css` 维护；`terminal-base.css` 保留旧工作台结构，`workspace.css` 只补充当前页面布局与明确的紧凑例外。后续新增表单不要另写随机的字号、高度和内边距。

| 用途 | 高度 | 说明 |
| --- | --- | --- |
| 表单输入、下拉、日期时间 | 36px | 字号13px，6px圆角，标签间隔6px |
| 主操作／保存表单 | 36px | 与表单控件同档 |
| 工具栏输入、筛选、普通按钮 | 32px | 同行对齐，控件间距8px |
| 数量快捷值、行情填价、跟随开关 | 28px | 保持紧凑，不压成细条 |
| 关闭、排序箭头、拖动手柄 | 32×32px | 图标按钮提供可访问名称 |
| 列配置中的列宽输入 | 32px | 与排序按钮同高，宽78px |
| 复选框 | 16×16px | 整个标签可点击，标签区域至少32px高 |
| 做T加减输入 | 外部36px | 两侧按钮、输入内部34px，共享1px外框 |

中文和普通文本用系统／苹方字体；金额、价格、数量、日期用SF Mono与等宽数字。涨跌配色为红涨（#c6414b）、绿跌（#48a579）、灰平（#8f9190），不影响操作按钮和错误色。

证券名称、表头排序、收益数值按钮不套标准按钮高度，保留双行表格布局及120/100px固定列。按钮禁用、输入禁用、只读、悬停、键盘焦点、无效字段状态在共享层定义；减少动态效果设置会关闭控件过渡。

录入和列设置弹窗使用内容自适应高度，不设置固定height；最大高度为760px与窗口高度减48px中的较小值。嵌入表单不拉伸填空；超过上限时只滚动内容，保留标题和操作区。短表单不为可能出现的额外字段预留空白。

提示统一使用 `FloatingNotice`：渲染到body的右上角堆叠区，最高层级1000，不影响页面/弹窗高度。每条可手动关闭；错误和警告不自动消失，成功信息3秒退出。关闭不清空业务错误值，不改变表单禁用、历史校验或重复成交保护。待对账消息仅在首次出现/状态重新出现时弹出，不随刷新重复提醒。长消息或多条消息仅提示区内部滚动，不能产生整屏滚动条。

表格走势列自动填充其他固定列占用后的剩余空间，最小165px；不再显示列宽输入或拖拽调宽手柄。图表SVG随列宽变化，保持56px高，不改变数据行高度。默认列顺序为走势、今日盈亏、本轮盈亏、其余行情指标、持仓市值、主力净流入；保留用户后续调整顺序的能力。

## 本轮审查修复

- `src/terminal-base.css` — 移除重复全局输入/按钮规则和交易输入19px特例；共享控件样式统一接管。
- `src/workspace.css` — 复选框不再继承全宽输入，分组六项操作不再错行，创建按钮与输入底部对齐。
- `src/workspace.css` — 列设置保留标题/保存区，只滚动字段列表；交易买卖操作区固定在表单底部；弹窗与抽屉内部滚动不传播给整页。
- `src/workspace.css` — 股票/ETF费率双面板使用相同字段网格、字号和间距，窄窗口改单列；备份页头部按钮与说明分行。
- `src/LedgerWorkbench.vue` — 嵌入资金/期初弹窗不重复模态层级；隐藏重复标题、关闭按钮与页脚，移除资金页重复新增入口；同步做T提示文字。
- `src/TradingDesk.vue` — 分组排序按钮增加明确标签，长名称提供提示；关闭按钮统一尺寸。
- `src/components/StepField.vue` — 可点击label与独立输入id关联，保留原加减与键盘操作。

检查参考 [Web Interface Guidelines](https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md) 的控件标签、焦点、禁用、滚动和减少动态效果规范；本轮不调整交易验证、费用或盈亏算法。

## 回归检查

- 添加自选：单一搜索输入、选中证券只读摘要、ETF类别和回转规则确认；更改搜索词清除旧选择，未选择不可添加；不保存测试证券。
- 自选/持仓：120px名称列、100px现价列和双行表头保持不变。
- 列设置：16px复选框、32px排序/列宽、固定保存按钮；不保存测试配置。
- 分组：六列同一行，名称与创建按钮底部对齐；不增删测试组。
- 成交：价格/数量/日期/备注/费用下拉均36px；实际费用三列等宽；手工价格不被刷新覆盖；不提交测试记录。
- 资金/期初/历史修改：同一套36px输入与日期；只有一组有效弹窗标题/关闭按钮；不修改历史记录。
- 做T：36px复合输入，按钮与输入对齐，反推下拉同高；键盘加减和手动锁定保留。
- 费率/备份：对齐、窄窗口和页面内部滚动；不保存测试费率或恢复数据。
- 已在浏览器930×1276、1280×720和720×700检查关键页面。`ControlStyles.test.ts` 验证CSS级联约束，不能替代真实布局测量；实机结果及当前包校验值见 `VALIDATION.md`。
